//!-----PRIMITIVE DATA TYPE-----
// Number
const age = 56;
const ageof = 56.6;

// String
const Name = "ritik";
const che = 'R';

// Boolean 
const isLoggedIn = true;
const isLoggedInOf = false;

// Undefined
let numOfChildren;

// Null
const salary = null;

//!-----NON-PRIMITIVE DATA TYPE-----
// Object
const user = {
    firstName: "Ritik",
    lastName: "Kumar",
    age: 22,
};

// Array
const mixedData = [10, "Ritik", true, 3.5, 'R', undefined, null,{}];
console.log(mixedData);
